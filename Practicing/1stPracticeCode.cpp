/*
Escribe una función que tome dos rectángulos (representados como 
std::pair<std::pair<double, double>, std::pair<double, double>>, 
donde el primer pair son las coordenadas del punto inferior izquierdo 
y el segundo pair son las coordenadas del punto superior derecho) y devuelva un 
std::pair<bool, std::pair<std::pair<double, double>, std::pair<double, double>>>. 
El bool indica si los rectángulos se intersectan, y el segundo pair 
representa las coordenadas del rectángulo de intersección (si existe).
*/

#include <iostream>
#include <cmath>
#include <utility> // Para std::pair

using RectInter = std::pair<std::pair<double, double>, std::pair<double, double>>;
using SolveRectInter = std::pair<bool, std::pair<std::pair<double, double>, std::pair<double, double>>>;

RectInter Rectangle_Intersection(double a, double b, double c, double d){

    

    return SolveRectInter;
}



main () {
    std::cout << "Hello World";
}